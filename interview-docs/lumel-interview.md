# Lumel

# Front end
bill split problem
Bill splitting problem and questions on anagram

How do you validate a SUDOKU (Given 2 D array)



# Back end
Round 1: Assessment
Round 2: Tech
Round 3: Tech

There were three rounds. 
The first was a difficult assessment where we had to manage sales data with all CRUD operations using Express, along with essential features like pagination and JWT. 
This was followed by two technical interviews. The first round was average, with mostly theoretical questions, 
some basic 'guess the output' questions, and database queries. 

The second interview involved medium-level theoretical questions and two problem-solving.

They asked about worker threads — what they are and how to implement them — along with a basic question about the call stack. 

The problem they gave me involved an array like [1,2,3,null,5,6,null,7,null,8], which needed to be divided into subarrays like [[1,2,3], [5,6], [7], [8]]. They also reframed the problem with different logic, such as setting a limit of 2 elements per subarray. Along with this in the first round, they asked to get the output of some of the object questions.


Write a script or API to load CSV data into mongodb database using express.js

design a database schema for attendance system and questions on querying the DB



# General
What is the difference between Budgeting and Forecasting?
Planning, Budgeting, Forecasting
Integrated planning



Coding round:
Easy question: In the string the unique characters should be decoded to '(' if there is duplicate decode into ')' 
Medium question: If the number is less than 1000 print as it is ,if it's above or 1000 print as 5k,6k,55k and similarly for 5M,2.5M and 1b,2.4b etc...
How do you validate a SUDOKU (Given 2 D array)
Channels - Fan-in/Fan-out patterns - Worker pools - JSON tokenized reading etc.

what is runtime env?
A Runtime Environment is the software layer that provides all the resources and services needed to execute a program written in a specific language.

req.params vs req.query
GET /user/12 - params
GET /user?id=123&sort=asc - query

what and use body-parser
body-parser is a middleware used in Express applications to parse the incoming request body and make it accessible under req.body.

By default, Node.js doesn't understand incoming request payloads like JSON or form data. 
body-parser helps decode and interpret them into usable JavaScript objects.


what security mechanisms available in node
